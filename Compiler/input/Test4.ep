def int gcd(int a, int b)
	if(a==b) then
		return (a) 
	fi;
	if(a>b) then
        int z;
		return(gcd(a-b,b))
	else
        int i;
		return(gcd(a,b-a))
    fi;
    double q;
    q = q + 3;
fed;
print(gcd(21,15));.
