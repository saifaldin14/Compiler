int z, y, f;
def double gcd(int a, double b)
    while (a==b) do
        return (a)
    od;

    return 5;
    int c;
    if (a>b or b<4.5 and a==0) then
        a = b;
        int d;
        double c;
        return(gcd(a-b,b))
    else
        int q;
        return(gcd(a,b-a))
    fi;
fed;

def double testDouble()
    int r;
    double r5, t6, y9;
    r5 = 3.4;
    t6 = 1.6;
    return r5 * t6;
fed;

int c;
print gcd(21,15);
print 45;
print 2*(gcd(21, 28) + 6).
