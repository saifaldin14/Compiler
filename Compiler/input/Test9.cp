def int gcd(int a, int b)
    while (a==b) do
        int r;
        return (a)
    od;

    int c;
    if (a>b) then
        int d;
        double c;
        return(gcd(a-b,b))
    else
        int r;
        return(gcd(a,b-a))
    fi;
fed;
int c;
print gcd(21,15);
print 45;
print 2*(gcd(21, 28) + 6).
