def int gcd(int a, int b)
    while (a==b) do
        return (a)
    od;

    return 5;
    int c;
    if (a>b or b<4 and a==0) then
        a = b;
        int d;
        double c;
        return(gcd(a-b,b))
    else
        int q;
        return(gcd(a,b-a))
    fi;
fed;
int c;
print gcd(21,15);
print 45;
print 2*(gcd(21, 28) + 6).
